const findShortestWordAmongMixedElements = (arr) => {
  const result = arr.filter(item => typeof item === 'string');
  return result.sort((a,b) => b.length - a.length).pop();
 });
 
 console.log(findShortestWordAmongMixedElements ([4, 'two', 2, 'three']));
