// Write a function called "getNthElement". Given an array and an integer, "getNthElement" returns the element at the given integer, within the given array. Notes: If the array has a length of 0, it should return 'undefined'.


var output = getNthElement([1, 3, 18, 9, 5], 4);

function getNthElement(array, n) {
    for (const element in array)
    return array[n]   
}

console.log(output); // --> 4
